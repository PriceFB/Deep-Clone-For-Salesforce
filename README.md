# Record-Super-Clone
A configurable deep-clone utility for Salesforce. Standard "Clone" only copies a single record and always copies all fields. Record Clone Plus lets admins configure which child related records to clone alongside the parent, and which fields to exclude, driven by Custom Metadata — no code changes required per object.
